[project]
name = "gradio-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Farana", email = "farana.alibhai@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.26.0",
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
gradio-app = "gradio_app:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
